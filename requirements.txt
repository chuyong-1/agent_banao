# requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# Core pipeline
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.0.0
typing-extensions>=4.9.0

# MCP server
mcp>=1.0.0

# LLM integration (optional — needed only when OPENAI_API_KEY is set)
langchain-openai>=0.2.0
openai>=1.0.0

# Utilities
python-dateutil>=2.8.0
